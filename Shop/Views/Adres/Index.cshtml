@model IEnumerable<Shop.Models.Adres>

@{
    ViewBag.Title = "Index";
}

<h2>Moje adresy</h2>

<p>
    @Html.ActionLink("Utwórz nowy", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Miasto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ulica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Numer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KodPocztowy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Miasto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ulica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Numer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KodPocztowy)
        </td>
        <td>
            @Html.ActionLink("Edycja", "Edit", new { id=item.AdresId }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.AdresId }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.AdresId })
        </td>
    </tr>
}

</table>
