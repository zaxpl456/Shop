@model IEnumerable<Shop.Models.Producent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Utwórz nowy", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edycja", "Edit", new { id=item.ProducentId }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.ProducentId }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.ProducentId })
        </td>
    </tr>
}

</table>
